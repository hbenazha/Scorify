<h1>Songs</h1>

<% @songs.each do |song| %>
  <p> <%= link_to "#{song.name}", song_path(song.id) %> </p>
<% end %>
<hr>
<h2>  <%= link_to "Add a new song", new_song_path  %> </h2>
<hr>
<h2> <%= link_to "Add a new user", new_user_path  %> </h2>
